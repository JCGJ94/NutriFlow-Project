{
    "buildCommand": "pnpm turbo run build --filter=@nutriflow/web",
    "framework": "nextjs",
    "installCommand": "NODE_ENV=development pnpm install --frozen-lockfile",
    "ignoreCommand": "exit 1"
}